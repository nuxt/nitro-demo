{
  "private": true,
  "scripts": {
    "build": "yarn generate && && cp .sls/node/server.js api/server.js",
    "generate": "nuxt generate"
  },
  "devDependencies": {
    "@nuxt/serverless": "^0.0.10",
    "nuxt": "^2.14.7"
  }
}
