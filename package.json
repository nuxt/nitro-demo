{
  "private": true,
  "scripts": {
    "build": "yarn genetate",
    "generate": "nuxt generate && cp .sls/node/server.js api/server.js"
  },
  "devDependencies": {
    "@nuxt/serverless": "^0.0.9",
    "nuxt": "^2.14.7"
  }
}
